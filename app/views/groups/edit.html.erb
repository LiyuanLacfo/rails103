<div class="col-md-4 col-md-offset-4">
	<%= form_for @group do |f| %>
		标题
		<br>
		<%= f.text_field :title %>
		<br>
		描述
		<br>
		<%= f.text_area :description %>
		<br>
		<%= f.submit "Submit", :disable_with => "Submitting..." %>
	<% end %>
</div>>